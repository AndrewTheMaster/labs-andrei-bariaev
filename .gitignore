# --- OS ---
.DS_Store
Thumbs.db
*~
*.swp
*.swo
.~lock.*

# --- IDE / редакторы ---
.idea/
.vscode/
*.code-workspace

# --- Go ---
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.prof
vendor/

# Скомпилированные бинари (go build ./cmd/...)
bench-runner
bench
lsh-demo
hashfs-bench
lsh-bench
perfecthash-bench

# --- Профилирование / артефакты замеров ---
*.cpu
*.mem
*_cpu.out
*_mem.out
cpu.out
mem.out
perf.data
perf.data.old

# --- LaTeX ---
*.aux
*.bbl
*.blg
*.fdb_latexmk
*.fls
*.lof
*.log
*.lot
*.out
*.synctex.gz
*.toc
*.xdv
*-blx.aux
*-blx.bib
*.run.xml
*.nav
*.snm
*.vrb

# Раскомментируй, если PDF не коммитишь:
# *.pdf

# --- Бенчмарки: сырые данные (генерируются `make collect`) ---
# PNG-графики в metrics/plots/ намеренно ВКЛЮЧЕНЫ в git — они нужны для REPORT.md на GitHub
metrics/raw/

# --- Крупные датасеты и черновики ---
lab1/AllCombined*.txt
results/

# Wikimedia / прочие дампы (локально, не в git)
ruwiki-*
enwiki-*
*-pages-articles.xml
*-pages-articles.xml.bz2
*.xml.bz2

# Lab 3: большой sift + venv локально (графики в metrics/plots/ коммитим)
lab-3-vecSearch/data/
lab-3-vecSearch/.venv/

# Lab 5: распакованный индекс и дампы в data/
lab-5-irindex/data/*
!lab-5-irindex/data/.gitignore
!lab-5-irindex/data/.gitkeep

# Go: coverage и бинари тестов в корне пакетов
coverage.out
**/coverage.out

# pprof (тексты top и PNG в plots/ остаются в git)
**/metrics/profiles/*.prof
